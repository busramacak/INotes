<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="check"
            type="com.bmprj.inotes.Check" />

    </data>

    <androidx.cardview.widget.CardView
        android:id="@+id/checkCardV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:background="@drawable/cardv_cerceve">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <CheckBox
                android:id="@+id/checkBox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight="90"
                android:text="@{check.check_text.toString(), default=DataBinding}"
                android:checked="@{check.is_checked == 1 ? true : false, default=false}"
                android:textColor="@{check.is_checked() == 1 ? @color/red : @color/black}"/>

            <ImageView
                android:id="@+id/deleteCheck"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight="10"
                android:layout_gravity="center"
                android:src="@drawable/delete"/>
        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>